# This is a Classy file.
# Single line comments come after a hash.

#| Multi-line comments start like that
|# And end on the same line as the bar and hash.

#| Comments can be nested #| like this |# ! Cool, huh? |#


if 0 {
	let bob = 5
	bob
} else {
	3
}
